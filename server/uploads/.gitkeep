# Runtime-uploaded and seed-generated images live here. The BYTES are never committed —
# uploaded files are user data; seed images are regenerated by `npm run db:seed`. Only this
# .gitkeep is tracked so the directory exists on a fresh clone (multer needs it to exist).
